#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* _strcmp - function that prints string
* @s1:"This is a parameter"
* @s2:"This is a parameter"
* Description: "the program's description"
* Return: Always 0 (Success)
*/

int _strcmp(char *s1, char *s2)
{int result;
result = strcmp(s1, s2);
return (result); }
