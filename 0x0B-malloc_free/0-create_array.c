#include <stdio.h>
#include "main.h"
/**
* create_array - writes character vc to stdout
* @size:"This is a parameter"
* @c:"This is a parameter"
* Description: "the program's description"
* Return: Always 0 (Success)
*/

char *create_array(unsigned int size, char c)
{ c[5] = {"S", "T", "A", "N", "D"};
if (sizeof(c) != 0)
{return NULL; }
else
*size = &c; }
