#include "main.h"
#include <stdio.h>
/**
 * _isupper - prints alphabets in lower case
 *@c:"c is a parameter"
 * Description: 'the program's description'
 *
* Return: Always 0 (Success)
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{return (1); }
else if (c >= 'a' && c <= 'z')
return (0);
}
