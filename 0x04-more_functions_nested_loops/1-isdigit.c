#include "main.h"
/**
 * _isdigit - prints alphabets in lower case
 *@c:"c is a parameter"
 * Description: 'the program's description'
 *
* Return: Always 0 (Success)
 */

int _isdigit(int c)
{return (c >= 0 && c <= 9;)}
