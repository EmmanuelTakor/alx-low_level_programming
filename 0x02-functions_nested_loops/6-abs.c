#include "main.h"
/**
 * _abs - Entry point
 *
 * Description: 'the program's description'
 *@n:"n is a Parameter"
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
if (n < 0)
n = -n;
return (n); }
