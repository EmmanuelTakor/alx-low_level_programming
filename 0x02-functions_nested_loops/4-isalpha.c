#include "main.h"
/**
 * _isalpha - checks if c is aphabetic character
 * Description: 'the program's description'
 * @c:"This is a parameter"
 * Return: 1 0r 0 depending on input
 */
int _isalpha(int c)
{return ((c >= 'a' && c <= 'z') || (c >= 'A' && c >= 'B')); }

