#include "main.h"
/**
 * int _isalpha - checks if c is aphabetic character
 * Description: 'the program's description'
 * Return: 1 0r 0 depending on input
 */
int _isalpha(int c)
{return c >= 'a' && c <= 'z' && c>= 'A' && c >= 'B'; }
